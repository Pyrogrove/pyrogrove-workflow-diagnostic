# Frozen Architecture

```text
User
-> Local Streamlit UI
-> Pydantic schemas
-> deterministic Python qualification
-> plain Python state machine
-> local JSON or SQLite state
-> audit trace
-> human confirmation and approval
-> Markdown or HTML report download
```

Verification:

```text
pytest
+ Ruff
+ Git evidence
```

MuleRun auxiliary uses only:

- adversarial synthetic cases;
- independent challenge;
- report, README, pitch, or diagram assistance;
- backup public presentation page.

No architecture debate is open at this stage.
